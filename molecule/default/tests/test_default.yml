---
file:
  /var/www/html/config.json:
    exists: true
    mode: "0640"
    owner: www-data
    group: www-data

service:
  alerta:
    enabled: true
    running: true

port:
  tcp:80:
    listening: true
    ip:
      - '0.0.0.0'
